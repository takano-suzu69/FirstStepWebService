// アプリケーション設定
const config = {
  apiUrl: 'https://[API_ID].execute-api.[REGION].amazonaws.com/prod',
  cognito: {
    userPoolId: '[userPoolId]',
    clientId: '[CLIENT_ID]',
    region: '[REGION]'
  }
};
